# TASKS.md — coworker 功能提升任务书

> 给负责功能提升的协作者（AI 或人类）。开工前先读 [AGENTS.md](AGENTS.md)（硬性约束）和 [UPGRADE_PLAN.md](UPGRADE_PLAN.md)（路线图）。
> 本文档把下一阶段的 4 个任务写清楚：**做什么、怎么算做完**。认领一个 → 开分支 → 自测 → 提交，**不要 merge**。
> 维护者：彭宇程（主线审查合并人）。

---

## 〇、全局要求（每个任务都必须满足，验收时逐条核验）

### A. 代码与架构
- [ ] 零依赖：不引入 npm 包、CDN 脚本、构建步骤；功能代码写在 `index.html` 单文件内
- [ ] 新增可调参数一律放进 `index.html` 顶部「参数区」（`AUTO_MIN`/`LOCATIONS`/`VARIETIES_BY`/`STATS_MANUAL` 附近），带中文注释说明口径与出处
- [ ] 农学参数必须有文献/推广机构口径，写进界面「展开」区与代码注释；**查不到就显示"暂无公开数据"，禁止编造**
- [ ] 复用现有管线：积温用 `gddSeries()`/`stageOf()`，图表手绘 SVG 参照 `renderF2`/`renderStats`，KPI 卡参照 `renderF5`，事件进月历走 `buildEvents()` 的 `add()`，行动项走 `buildPlan()` 的 `A.push({date,lv,title,why,how})`

### B. 双语（最容易漏，曾出过线上 bug）
- [ ] 所有新 UI 文案必须走 `T(中文, 英文)`；静态区块要在 `applyStaticLang()` 里注册双语（参照 `#stats` 区块的写法）
- [ ] 切到 EN 后，新模块**一个中文字都不能残留**（含 title 提示、SVG 内文字、图表标注）
- [ ] 中→英→中往返后，新旧模块文案均正确（导航事件曾因类似问题返工，勿重蹈）

### C. 健壮性
- [ ] `file://` 直接双击打开：控制台零报错、新模块优雅降级（参照 `renderStats()` 的降级写法）
- [ ] 新增 localStorage 数据使用 `cacheGet/cacheSet` 并带缓存键版本意识（数据结构变更时把 `CACHE_VER` +1）
- [ ] 改了 `index.html` 后：`sw.js` 的 `CACHE_VERSION` +1，且更新 `applyStaticLang` 里的版本号文案与 `WELCOME_VER`（若属用户可感知的新版本）
- [ ] 手机宽度 375px 无横向滚动条；新触控目标 ≥44px

### D. 提交与文档
- [ ] 分支名 `feat/<功能名>`（从 `upgrade/v4` 拉出）；一个功能一个提交，中文 message，格式 `feat(模块): 一句话` + 空行 + 要点
- [ ] 「更新日志」区块加条目（中英双语，`applyStaticLang` 内）
- [ ] 提交前自测命令全部通过：
  ```bash
  python -c "import re;html=open('index.html',encoding='utf-8').read();open('tools/_check.js','w',encoding='utf-8').write(re.search(r'<script>(.*)</script>',html,re.S).group(1))"
  node --check tools/_check.js && rm tools/_check.js   # 内联脚本语法
  node --check sw.js                                    # 改过 sw 才需要
  ```
- [ ] 汇报格式：改了哪些文件 / 每条验收标准的自测证据（截图或控制台输出）/ 遗留问题与取舍说明

---

## 任务一：物候人工修正（"我家今天初花"）　★最高优先级

**背景**：全站日期都靠积温推算盛花日（`G.bloom`），模式数据在山区有 1–2℃ 偏差。桃农站在园里看到真花，比任何模型都准。

**需求**
1. 在「花期防冻」卡片内加一个入口：「我家今天初花/盛花了」按钮 + 次级选项（现蕾/露瓣/初花/盛花，对应 `STAGES`）
2. 点击后以用户提交的日期为准重算：各品种采收窗口、疏果/套袋节点、冻害阶段判定（即 `G.bloom`（或对应物候锚点）被用户值覆盖）
3. 覆盖关系持久化到 localStorage（含提交日期与所选阶段），换品种/重置入口保留「恢复气象推算」
4. 被覆盖时，界面在防冻卡、采收卡、月历显著位置标注「按您上报的物候计算」（双语）
5. 若上报日期明显反常（如 9 月报初花），弹确认但不阻止

**实现提示**：`G.bloom` 的产生在 `refreshAll()`；下游全部读它，所以在赋值处统一收口最省事。存 localStorage 用键 `phenologyOverride:<G.loc>`（分产区）。

**验收标准**
- [ ] 上报盛花后：`buildEvents()`、`buildPlan()`、`renderF3()`、`updateHero()` 输出的日期全部随之改变（举一个品种核对比对前后截图）
- [ ] 刷新页面、切换产区再切回，覆盖依然生效；点「恢复推算」后回到积温值
- [ ] EN 模式下入口、标注、确认弹窗全部英文
- [ ] file:// 打开可用；清 localStorage 后无残留
- [ ] 9 月上报初花出现确认提示

---

## 任务二：虫害风险模块（梨小食心虫为主）　★高优先级

**背景**：平谷桃农第二头疼的是虫（梨小食心虫钻袋、蚜虫、红蜘蛛）。积温预测各代成虫高峰是成熟方法，且能复用现有 GDD 管线。

**需求**
1. 新增第六张决策卡「哪几天要防虫」：以越冬代幼虫上芽的有效积温（基数待查文献）预测 1–3 代成虫峰，未来 16 天内给出「打药窗口」日
2. 至少覆盖：梨小食心虫（主）、蚜虫（物候期粗判即可：展叶–新梢期）
3. 阈值出处必须写明（如：北京/河北植保站、IPM 指南、文献页码），写入「科学依据」展开区；查不到的代数显示"暂无公开数据"
4. 行动项进推进方案（`lv:"yel"`，how 里写"具体药剂听植保站"）、风险日进月历（新标签色 + 图例）
5. 参数区暴露：各代积温阈值、起始日期（盛花锚点或越冬基数日），便于科技小院后续校准

**实现提示**：起始锚点接 `G.bloom`（被任务一的物候修正覆盖后自动跟随——若任务一未合并，用 `G.bloom` 即可，留 TODO）。计算写纯函数（输入 GDD 序列+阈值，输出峰日数组），便于单测。

**验收标准**
- [ ] 当前季节（9 月）能输出至少一条"下一代防治窗口"行动项；2–5 月模拟（把 `G.today` 临时改小自测）出现越冬代提醒
- [ ] 每个阈值在界面上可查到出处；无出处的代数明确显示"暂无公开数据"
- [ ] 月历出现虫害标签且图例有对应项；EN 模式无中文残留
- [ ] 纯函数部分附 3 组手算对照数据（写在提交信息或注释里）

---

## 任务三：多园管理（多地块）　中优先级

**背景**：不少农户在两个镇都有园子，目前切产区要来回点，且看不到两边"今天各要干什么"。

**需求**
1. 允许"收藏"多个产区（产区下拉旁加 ⭐，存 localStorage）
2. 顶部（hero 速览卡下方）加一条「我的园子」摘要条：收藏的每个产区一行——今日最低温/降雨 + 该区**最紧急的一条行动项**（取自该区 `buildPlan()` 的第一条 red/yel）
3. 点击某行跳到该产区并刷新数据
4. 收藏状态在刷新后保留；未收藏时摘要条隐藏

**实现提示**：行动项需要在**不改 `G` 全局状态**的情况下为其他产区计算——把 `refreshAll` 的"取数+算 GDD+盛花"抽成纯函数 `loadRegion(locIndex)`（返回区域数据对象），`buildPlan`/`buildEvents` 接受可选的区域数据参数，默认仍用 `G`。这是本次任务的主要重构点，注意回归。

**验收标准**
- [ ] 收藏 2 个产区后，摘要条同时显示两区今日关键信息与紧急行动项，数据与各自单产区页面一致（抽查一个日期核对）
- [ ] 点击行能正确跳转；取消收藏后条目消失
- [ ] 原有单产区流程无回归：切产区、断网兜底、自动刷新（30 分钟）均正常
- [ ] 375px 宽度下摘要条纵向堆叠不溢出
- [ ] EN 模式下摘要条全英文

---

## 任务四：数据导出备份　中优先级

**背景**：任务一落地后，用户上报的物候、（未来的）实测记录都在 localStorage——换手机就丢。给一个"导出/导入"口。

**需求**
1. 「说明」区块底部加「我的记录」卡：显示当前 localStorage 里与本站相关的键清单及大小
2. 「导出」：打包相关键为 JSON 下载（文件名含日期）；「导入」：选择文件校验结构后写回并刷新
3. 导入前弹出确认，说明会覆盖现有记录；结构不符时拒绝并提示
4. 导出的 JSON 顶层带 `app:"taoyuan"` 与版本号，供未来兼容判断

**验收标准**
- [ ] 导出→清空 localStorage→导入，物候覆盖等状态完整恢复
- [ ] 导入损坏 JSON / 结构不符文件：弹错误提示，原数据不受影响
- [ ] 下载与文件选择在手机浏览器可用（至少 Chrome Android 验证口径；iOS 不支持选择文件时给出提示）
- [ ] EN 双语完整

---

## 二、优先级与排期建议

| 任务 | 优先级 | 预估工作量 | 依赖 |
|---|---|---|---|
| 一 物候人工修正 | ★★★ | 1–2 天 | 无 |
| 二 虫害风险 | ★★★ | 2–3 天 | 建议在一之后（共享锚点） |
| 三 多园管理 | ★★ | 2–3 天 | 无（重构注意回归） |
| 四 数据导出 | ★★ | 1 天 | 一（有数据可导才有意义） |

建议顺序：一 → 二 → 四 → 三。每完成一个任务即提交一次 PR，不要攒。

## 三、验收流程（维护者执行）

1. 对照任务验收标准逐条核验 + 全局 A–D 清单抽验
2. 真实浏览器过一遍：中文/英文往返、375px 宽、断网刷新、file:// 打开
3. 农学参数抽查出处（查不到出处的整块打回）
4. 通过则合并进 `upgrade/v4`；发现问题的，在 PR 里用「问题 + 建议改法」批注退回

## 四、红线（违反直接打回）

- 编造农学数字或出处
- 引入运行时依赖/构建步骤/统计埋点
- 修改 `functions/api/stats.js` 的密钥处理方式（密钥只允许留在服务端环境变量）
- 只交代码不交自测证据
